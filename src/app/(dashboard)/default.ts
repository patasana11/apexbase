// Force client-side rendering for all dashboard routes
export const dynamic = 'force-dynamic';
export const runtime = 'edge';  // Using edge runtime for better performance
export const preferredRegion = 'auto';

// Disable React Server Components completely for this route segment
export const RSC = false;
